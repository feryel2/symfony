{% extends '/frontoffice/basefront.html.twig' %}

{% block title %}Remboursement index{% endblock %}
{% block subtitle %}Remboursement {% endblock %}

{% block content %}
<<section class="pt-125 pb-140 bg_white">
    <div class="container">
        <div class="row ">
            <div class="section-title">
                <h2 class="heading-decorated">Remboursement Details</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ remboursement.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ remboursement.Montant }}</td>
            </tr>
            <tr>
                <th>DateRemboursement</th>
                <td>{{ remboursement.DateRemboursement ? remboursement.DateRemboursement|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a style="max-width: 300px;" class="btn btn-block btn-outline-info" href="{{ path('app_remboursement_list', {'idcredit': remboursement.credit.id}) }}">Back to list</a>

    {# <a href="{{ path('app_remboursement_edit', {'id': remboursement.id}) }}">edit</a> #}

    {{ include('remboursement/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</section>
{% endblock %}
