{% extends '/frontoffice/basefront.html.twig' %}

{% block title %}Credit index{% endblock %}
{% block subtitle %}Modifier Credit{% endblock %}

{% block content %}
<section class="pt-125 pb-140 bg_white">
    <div class="container">
        <div class="row ">
            <div class="section-title">
        <h2 class="heading-decorated">Modifier Credit</h2>

    {{ include('credit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_credit_index') }}">back to list</a>

    {{ include('credit/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</section>
{% endblock %}
